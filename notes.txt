lo ideal es que tanto la API no sepa nada de la UI, como que la UI no sepa nada de la API.
Para lograr eso, tenés a un tercero (en mi ejemplo creo que es index.js) que es el que sabe de ambas.
La API pide cosas a la API y devuelve JSON (pero no sabe para qué se va a usar), la UI espera JSON para construir la interfaz (pero no sabe de dónde viene ese JSON).
El index pide data a la API y se la pasa a la UI, hace el nexo

entender mi codigo de nuevo....

va a haber problemas cuando llamo display pokemons con otro numero de agina, hacerlo de alguna forma desde index
el page-- en displaypokemons no va a hacer falta creo
